import 'package:flutter/material.dart';

class IndexSummary extends StatelessWidget {
  const IndexSummary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(child: Column(
        
      )),
    );
  }
}